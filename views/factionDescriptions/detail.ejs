<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= faction.name %> Description</title>
</head>
<body>
    <header>
        <nav>
            <ul>
                <li><a href="/">Home</a></li>
                <li><a href="/render/battle-systems">Battle Systems</a></li>
            </ul>
        </nav>
    </header>

    <h1><%= faction.name %></h1>
    <% if (faction.imageUrl) { %>
        <img src="<%= faction.imageUrl %>" alt="Image of <%= faction.name %>" style="max-width: 300px;">
    <% } %>
    <h2>Description</h2>
    <% if (faction.description && faction.description.dataValues.description) { %>
        <p><%= faction.description.dataValues.description %></p>
    <% } else { %>
        <p>No description available.</p>
    <% } %>
    <br>
    <a href="javascript:history.back()">Back to Previous Page</a>
</body>
</html>