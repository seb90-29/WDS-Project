<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Add New Faction</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Times New Roman', Times, serif;
            background-color: #000000;
            color: #f0f0f0;
            margin: 0;
            padding: 0;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            min-height: 100vh;
            background-image: url('/visual pictures/Main.jpg');
            background-size: contain;
            background-repeat: no-repeat;
            background-position: center;
            background-attachment: fixed;
        }

        h2 {
            font-family: 'Garamond', serif;
            font-size: 2.5em;
            color: #ffffff;
            margin-bottom: 0.5em;
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.7);
            text-align: center;
        }

        .container {
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        form {
            background: rgba(0, 0, 0, 0.7);
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0px 0px 20px rgba(0, 0, 0, 0.5);
            display: flex;
            flex-direction: column;
            align-items: center;
            max-width: 90%;
            margin: 20px;
        }

        label {
            font-family: Arial, sans-serif;
            font-size: 1.25em;
            color: #dddddd;
            margin-bottom: 10px;
        }

        input,
        textarea {
            font-size: 1.25em;
            padding: 10px;
            margin-bottom: 20px;
            border: none;
            border-radius: 5px;
            width: 100%;
            max-width: 400px;
        }

        button {
            font-family: Arial, sans-serif;
            font-size: 1.25em;
            color: #ffffff;
            background-color: #555555;
            padding: 10px 20px;
            border-radius: 5px;
            border: none;
            cursor: pointer;
            transition: background-color 0.3s ease, transform 0.3s ease;
            box-shadow: 2px 2px 10px rgba(0, 0, 0, 0.5);
            margin-top: 10px;
        }

        button:hover {
            background-color: #777777;
            transform: scale(1.05);
        }

        a {
            font-family: Arial, sans-serif;
            font-size: 1.25em;
            color: #ffffff;
            background-color: #555555;
            padding: 10px 20px;
            border-radius: 5px;
            text-decoration: none;
            transition: background-color 0.3s ease, transform 0.3s ease;
            display: inline-block;
            margin-top: 1em;
            box-shadow: 2px 2px 10px rgba(0, 0, 0, 0.5);
        }

        a:hover {
            background-color: #777777;
            transform: scale(1.05);
        }

        @media (max-width: 768px) {
            h2 {
                font-size: 2em;
            }

            form,
            a {
                font-size: 1em;
                padding: 8px 16px;
            }
        }
    </style>
</head>

<body>
    <div class="hero">
        <h2>Add New Faction</h2>
        <div class="container">
            <form action="/render/factions/battle-system/<%= battleSystemId %>" method="post" enctype="multipart/form-data">
                <label for="name">Faction Name:</label>
                <input type="text" id="name" name="name" required>

                <label for="description">Description:</label>
                <textarea id="description" name="description" required></textarea>

                <label for="image">Faction Image:</label>
                <input type="file" id="image" name="image" accept="image/*">
                <button type="button" onclick="document.getElementById('image').value = '';">Cancel Upload</button>

                <button type="submit">Add Faction</button>
            </form>
            <a href="/render/factions?battleSystemId=<%= battleSystemId %>">Back to Factions</a>
            <a href="javascript:history.back()">Back to Previous Page</a>
        </div>
    </div>
</body>

</html>
