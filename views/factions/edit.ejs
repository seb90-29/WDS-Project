<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Faction - <%= faction.name %></title>
</head>
<body>
    <header>
        <nav>
            <ul>
                <li><a href="/">Home</a></li>
                <li><a href="/render/battle-systems">Battle Systems</a></li>
            </ul>
        </nav>
    </header>

    <h1>Edit Faction - <%= faction.name %></h1>
    <form action="/render/factions/<%= faction.id %>/edit" method="post" enctype="multipart/form-data">
        <label for="name">Faction Name:</label>
        <input type="text" id="name" name="name" value="<%= faction.name %>" required>

        <label for="description">Description:</label>
        <textarea id="description" name="description" required><%= faction.description ? faction.description.description : '' %></textarea>

        <label for="image">Faction Image:</label>
        <% if (faction.imageUrl) { %>
            <img src="<%= faction.imageUrl %>" alt="Image of <%= faction.name %>" style="max-width: 300px;">
            <input type="hidden" name="currentImageUrl" value="<%= faction.imageUrl %>">
        <% } %>
        <input type="file" id="image" name="image" accept="image/*">
        <button type="button" onclick="document.getElementById('image').value = '';">Cancel Upload</button>

        <button type="submit">Update Faction</button>
    </form>

    <a href="javascript:history.back()">Back to Previous Page</a>
</body>
</html>