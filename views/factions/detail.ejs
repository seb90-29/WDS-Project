<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= faction.name %> Description</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Times New Roman', Times, serif;
            background-color: #000000;
            color: #f0f0f0;
            margin: 0;
            padding: 0;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            min-height: 100vh;
            background-image: url('/visual pictures/Main.jpg');
            background-size: contain;
            background-repeat: no-repeat;
            background-position: center;
            background-attachment: fixed;
        }

        .hero {
            text-align: center;
            padding: 50px;
            background: rgba(0, 0, 0, 0.7);
            border-radius: 10px;
            box-shadow: 0px 0px 20px rgba(0, 0, 0, 0.5);
            max-width: 90%;
            margin: 20px;
        }

        h1 {
            font-family: 'Garamond', serif;
            font-size: 2.5em;
            color: #ffffff;
            margin-bottom: 0.5em;
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.7);
        }

        h2 {
            font-family: Arial, sans-serif;
            font-size: 2em;
            color: #dddddd;
            margin-bottom: 1em;
        }

        p {
            font-family: Arial, sans-serif;
            font-size: 1.25em;
            color: #dddddd;
            max-width: 800px;
            text-align: center;
            margin-bottom: 2em;
            line-height: 1.6;
            text-shadow: 1px 1px 3px rgba(0, 0, 0, 0.7);
            margin: 0 auto;
        }

        img {
            max-width: 90%;
            height: auto;
            margin-bottom: 2em;
            border-radius: 10px;
            box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.7);
            display: block;
            margin-left: auto;
            margin-right: auto;
        }

        .actions {
            display: flex;
            justify-content: center;
            gap: 15px;
            margin-top: 20px;
        }

        a,
        button {
            font-family: Arial, sans-serif;
            font-size: 1.25em;
            color: #ffffff;
            background-color: #555555;
            padding: 10px 20px;
            border-radius: 5px;
            text-decoration: none;
            transition: background-color 0.3s ease, transform 0.3s ease;
            display: inline-block;
            box-shadow: 2px 2px 10px rgba(0, 0, 0, 0.5);
            border: none;
            cursor: pointer;
        }

        a:hover,
        button:hover {
            background-color: #777777;
            transform: scale(1.05);
        }

        .delete-button {
            background-color: #c0392b;
        }

        .delete-button:hover {
            background-color: #e74c3c;
        }

        form {
            display: inline;
        }

        @media (max-width: 768px) {
            h1 {
                font-size: 2em;
            }

            h2 {
                font-size: 1.5em;
            }

            p,
            a,
            button {
                font-size: 1em;
                padding: 8px 16px;
            }

            img {
                max-width: 100%;
                height: auto;
            }

            .actions {
                flex-direction: column;
                align-items: center;
            }
        }
    </style>
</head>

<body>
    <div class="hero">
        <h1><%= faction.name %></h1>
        <h2>Description</h2>
        <% if (faction.description && faction.description.description) { %>
            <p><%= faction.description.description %></p>
        <% } else { %>
            <p>No description available.</p>
        <% } %>
        <% if (faction.description && faction.description.imageUrl) { %>
            <img src="<%= faction.description.imageUrl %>" alt="Image of <%= faction.name %>">
        <% } else { %>
            <p>No image available.</p>
        <% } %>

        <div class="actions">
            <a href="/render/factions/<%= faction.id %>/edit">Edit Faction</a>
            <form action="/render/factions/<%= faction.id %>/delete" method="post">
                <button type="submit" class="delete-button" onclick="return confirm('Are you sure you want to delete this faction?')">Delete Faction</button>
            </form>
            <a href="javascript:history.back()">Back to Previous Page</a>
        </div>
    </div>
</body>

</html>

