<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= faction.name %> Description</title>
</head>
<body>
    <header>
        <nav>
            <ul>
                <li><a href="/">Home</a></li>
                <li><a href="/render/battle-systems">Battle Systems</a></li>
            </ul>
        </nav>
    </header>

    <h1><%= faction.name %></h1>
    <h2>Description</h2>
    <% if (faction.description && faction.description.description) { %>
        <p><%= faction.description.description %></p>
    <% } else { %>
        <p>No description available.</p>
    <% } %>
    <% if (faction.description && faction.description.imageUrl) { %>
        <img src="<%= faction.description.imageUrl %>" alt="Image of <%= faction.name %>" style="width: 1024px; height: 1024px;">
    <% } else { %>
        <p>No image available.</p>
    <% } %>
    <br>
    <a href="/render/factions/<%= faction.id %>/edit">Edit Faction</a>
    <br>
    <form action="/render/factions/<%= faction.id %>/delete" method="post" style="display:inline;">
        <button type="submit" onclick="return confirm('Are you sure you want to delete this faction?')">Delete Faction</button>
    </form>
    <br>
    <a href="javascript:history.back()">Back to Previous Page</a>
</body>
</html>